// Editor integration (MathQuill / query editor) placeholder
document.addEventListener('DOMContentLoaded', () => {
  const ta = document.querySelector('#query');
  if (ta) {
    // placeholder behavior
    ta.addEventListener('input', () => {
      // noop
    });
  }
});
